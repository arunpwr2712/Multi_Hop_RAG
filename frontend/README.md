# Next.js Frontend

Frontend for the Multi-hop Causal RAG system with:

- Chatbot interaction
- Candidate causal chain view
- Interactive graph view

## Run

```bash
cd frontend
npm install
npm run dev
```

Create `.env.local` if needed:

```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```
